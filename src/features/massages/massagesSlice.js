import { createSlice } from "@reduxjs/toolkit";
const initialState={

}
const massagesSlice = createSlice({
   name:"conversations",
   initialState,
   reducers:{}
})
export const {}=massagesSlice.actions;
export default massagesSlice.reducer;